services:
  - type: web
    name: neural-messenger
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt && playwright install chromium
    startCommand: python app.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
